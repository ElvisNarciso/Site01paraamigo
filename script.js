function clickMenu(){
    if(itens.style.display == 'flex'){
        itens.style.display = 'none';
    } else {
        itens.style.display = 'flex';
    }
};

// let clickMenu = document.querySelector('#itens');
// if(clickMenu.classList.)